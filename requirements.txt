numpy>=1.26
pandas>=2.0
scipy>=1.11

# real-data adapters (fda_alpha/realdata): openFDA / CT.gov / Yahoo HTTP.
# pyarrow is optional — the cache falls back to CSV if it is absent.
requests>=2.31
pyarrow>=14.0

# optional: webhook server (falls back to stdlib http.server if absent)
fastapi>=0.110
uvicorn>=0.27
